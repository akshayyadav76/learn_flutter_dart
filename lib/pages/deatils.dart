import 'package:flutter/material.dart';

class Deatils extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("easylit")),
        body: Center(child: Text("this the deailts of it"),));
  }
}
